module Kernel
  def getb
    $stdin.readbyte
  rescue EOFError
    -1
  end
end

class <%= class_name %>
  def initialize
    @buffer = +"".b
    @pos = 0
    @cur = 0
    @level = 0
    @lrstack = []
    @thunks = []
    @lrtable = LrTable.new
  end
end

require_relative "<%= prefix%>.so"
